# CLAUDE.md — (Flint) Github 3d Visualizer

**Start at `Mesh/(System) Flint Init.md`.** It is the contract for this workspace.

This vault documents the repository at `/Users/brunojaamaa/Desktop/github-3d-visualizer`,
publicly known as the **3D GitHub Visualizer**.

> ⚠️ The Flint is registered as **`Github 3d Visualizer`** because `flint init` rejects both
> `GitHub` (mixed case) and `3D` (uppercase after a digit). The project's real name is
> **3D GitHub Visualizer**. See [[(Report) Build Log]].

**The repo is the truth for code. This vault is the truth for state.** Where they disagree,
the repo wins and the note gets fixed.

Up one level: [[(Guide) BRUNO HQ]] → `/Users/brunojaamaa/Desktop/Main Vault/Main/Mesh/(Map) BRUNO HQ.md`.

Never run destructive git operations here. This vault describes the repo; it does not own it.
