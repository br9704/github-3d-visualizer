node_modules
dist
.env
.env.local
*.log
npm-debug.log
.DS_Store

# Editor / IDE
.vscode
.idea

# Agent + coordination wiring — machine-specific absolute paths, never ship these
.mcp.json
.claude/settings.json
.claude/settings.local.json
.cursor/
.codex/
opencode.json
AGENTS.md
GEMINI.md

# Local verification artifacts
playwright-report/
test-results/
shots/
artifacts/

# Agent-process prompts. The masterplan is this project's record of how it was
# built; the prompts that drove a session are not, and CLAUDE.md is explicit
# that process artifacts do not get committed here.
DOCS-ENGINEERPROMPT.md

# Vercel CLI link (project + org ids, machine-local)
.vercel

# --- machine-local agent + secret config (uniform block) ---
# .env, .mcp.json, opencode.json, .cursor/, .codex/, .vercel and .vscode are
# already ignored above; only the lines this repo was missing are added here.
.env.*
!.env.example
.aethereum*
.claude/
*-ENGINEERPROMPT.md
ENGINEERPROMPT.md
